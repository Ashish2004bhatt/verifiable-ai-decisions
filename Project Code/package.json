{
  "name": "verifiable-ai-decisions",
  "version": "1.0.0",
  "description": "Tamper-proof, privacy-preserving proof of AI decisions for healthcare",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../contracts && npm install",
    "build": "cd frontend && npm run build",
    "test": "cd contracts && npm test"
  },
  "keywords": ["ai", "blockchain", "healthcare", "verification"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

